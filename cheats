-- (Knihovna Rayfield zůstává stejná)
-- ... (zde vlož zbytek Rayfield kódu)

-- Přidej tuto funkci pro "Silent" zásah
local function getBestTarget()
    local best = nil
    local dist = config.fov
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
            -- Team check
            if config.teamCheck and p.Team == LocalPlayer.Team then continue end
            
            local pos, onScreen = Camera:WorldToViewportPoint(p.Character.Head.Position)
            if onScreen then
                local d = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                if d < dist then
                    best = p.Character.Head
                    dist = d
                end
            end
        end
    end
    return best
end

-- Hook na střelbu (to, co dělá ty hity i mimo postavu)
local old; old = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    if config.silentAim and (method == "FindPartOnRay" or method == "FindPartOnRayWithIgnoreList") then
        local target = getBestTarget()
        if target then
            args[1] = Ray.new(args[1].Origin, (target.Position - args[1].Origin).Unit * 500)
            return old(self, unpack(args))
        end
    end
    return old(self, ...)
end)
