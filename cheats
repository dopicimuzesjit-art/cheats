--[[ 
    OPTIMALIZOVANÝ SCRIPT 
    Poznámka: Pro nejlepší funkčnost přejmenuj soubor na .lua
]]

local config = {
    aimbot = true,
    wallhack = true,
    esp = true,
    aimbotFOV = 100,
    smoothness = 0.2 -- Nižší číslo = plynulejší pohyb
}

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- Pomocná funkce pro nalezení nejbližšího hráče
local function getClosestPlayer()
    local closest = nil
    local shortestDistance = config.aimbotFOV

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            local pos, onScreen = Camera:WorldToViewportPoint(player.Character.HumanoidRootPart.Position)
            if onScreen then
                local distance = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                if distance < shortestDistance then
                    closest = player.Character.HumanoidRootPart
                    shortestDistance = distance
                end
            end
        end
    end
    return closest
end

-- Hlavní smyčka
RunService.RenderStepped:Connect(function()
    -- AIMBOT & SOFT AIM (Kombinované pro stabilitu)
    if config.aimbot then
        local target = getClosestPlayer()
        if target then
            local targetCFrame = CFrame.lookAt(Camera.CFrame.Position, target.Position)
            Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, config.smoothness)
        end
    end

    -- ESP & WALLHACK
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            -- Wallhack (Průhlednost)
            if config.wallhack then
                for _, part in pairs(player.Character:GetChildren()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                        part.Transparency = 0.5
                    end
                end
            end

            -- Jednoduchý ESP Box
            if config.esp then
                if not player.Character:FindFirstChild("ESP_Box") then
                    local box = Instance.new("BoxHandleAdornment")
                    box.Name = "ESP_Box"
                    box.AlwaysOnTop = true
                    box.ZIndex = 5
                    box.Adornee = player.Character
                    box.Color3 = Color3.fromRGB(255, 0, 0)
                    box.Size = player.Character:GetExtentsSize()
                    box.Transparency = 0.7
                    box.Parent = player.Character
                end
            end
        end
    end
end)

print("Script úspěšně načten!")
